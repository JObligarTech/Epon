import { ScreenStub } from "@/components/shell/ScreenStub";

export default function HomePage() {
  return (
    <ScreenStub
      title="How much you have, where it is, what changed."
      description="The overview lands next: net position, where your cash sits across institutions, this month's spending, and recent activity."
    />
  );
}
