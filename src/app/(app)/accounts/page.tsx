import { ScreenStub } from "@/components/shell/ScreenStub";

export default function AccountsPage() {
  return (
    <ScreenStub
      title="Every account, grouped by who holds it."
      description="Balances, available versus current, masked numbers, connection status and last sync — arriving with the accounts screen."
    />
  );
}
